import { colors } from "./colors";
import { fonts, fontWeights, fontSizes } from "./fonts";
import { space, lineHeights } from "./scaping";
import { sizes } from "./sizes";
export const theme = {
  colors,
  fontSizes,
  fontWeights,
  fonts,
  space,
  lineHeights,
  sizes
};
